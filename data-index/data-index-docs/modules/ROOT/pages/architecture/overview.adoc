= How Data Index Works
:page-aliases:

Data Index is a read-only query service that captures and normalizes workflow execution events from Quarkus Flow applications.

== High-Level Architecture

Data Index supports two storage backends, each with different characteristics:

[source,text]
----
Quarkus Flow App
    ↓ (structured logging → stdout)
FluentBit DaemonSet
    ↓ (tail container logs)
Storage Backend
    ├─ PostgreSQL (triggers) → < 1ms normalization
    └─ Elasticsearch (transforms) → ~1s normalization
    ↓
GraphQL API
----

== Storage Backends

Data Index provides two production-ready storage options:

[cols="1,2,2"]
|===
|Backend |Best For |Architecture

|**PostgreSQL**
|< 50K workflows/day, ACID transactions, simple deployment
|xref:architecture/postgresql-mode.adoc[PostgreSQL Mode Details]

|**Elasticsearch**
|100K+ workflows/day, full-text search, analytics
|xref:architecture/elasticsearch-mode.adoc[Elasticsearch Mode Details]

|===

Choose based on your requirements:
- **Need ACID transactions?** → PostgreSQL
- **Need full-text search?** → Elasticsearch
- **High throughput (>50K/day)?** → Elasticsearch
- **Simple deployment?** → PostgreSQL

== Key Components

=== 1. Quarkus Flow Applications

Applications with https://docs.quarkiverse.io/quarkus-flow/dev/structured-logging.html[structured logging^] enabled write JSON events to stdout:

[source,json]
----
{"instanceId":"01KQ...", "eventType":"io.serverlessworkflow.workflow.started.v1", "timestamp":1777298089.549604, ...}
----

**Critical configuration:**

* `quarkus.flow.structured-logging.enabled=true`
* `quarkus.flow.structured-logging.timestamp-format=epoch-seconds`
* Console handler for JSON output

=== 2. FluentBit DaemonSet

FluentBit runs on each Kubernetes node and:

* Tails `/var/log/containers/*_workflows_*.log` (pods in `workflows` namespace)
* Filters lines to extract only JSON events (not regular app logs)
* Parses JSON and extracts fields
* Forwards to storage backend (PostgreSQL or Elasticsearch)

**Configuration:**

* `fluent-bit.conf` - Input, filter, output configuration
* `flatten-event.lua` - Lua script to flatten nested JSON
* Deployed via ConfigMap + DaemonSet

=== 3. Storage Backend

==== PostgreSQL Mode

* **Raw tables** - Store complete events as JSONB
* **Triggers** - Extract fields and UPSERT to normalized tables (< 1ms)
* **Normalized tables** - Optimized for querying

See xref:architecture/postgresql-mode.adoc[PostgreSQL Mode Architecture] for details.

==== Elasticsearch Mode

* **Raw indices** - Store complete events
* **ES Transforms** - Extract fields and write to normalized indices (~1s)
* **Normalized indices** - Optimized for querying and search

See xref:architecture/elasticsearch-mode.adoc[Elasticsearch Mode Architecture] for details.

=== 4. Data Index Service

Quarkus application providing:

* **GraphQL API** - Query workflow instances and task executions
* **Storage adapter** - JPA for PostgreSQL, ES Client for Elasticsearch
* **SmallRye GraphQL** - GraphQL schema and resolvers
* **Health checks** - Liveness and readiness probes

== Event Lifecycle

1. **Workflow executes** in Quarkus Flow app
2. **Structured logging** writes JSON event to stdout
3. **Kubernetes** captures stdout to `/var/log/containers/POD_NAME.log`
4. **FluentBit** tails log file, extracts JSON events
5. **Storage backend** receives events:
   - **PostgreSQL**: INSERT triggers fire, normalize immediately
   - **Elasticsearch**: ES Transform processes asynchronously
6. **Data Index** queries normalized data via storage adapter
7. **GraphQL** returns data to user

== Event Processing Time

[cols="1,2,2"]
|===
|Metric |PostgreSQL |Elasticsearch

|**Normalization**
|< 1ms (triggers)
|~1s (ES Transform)

|**End-to-end**
|5-10 seconds
|5-10 seconds

|**FluentBit flush**
|1 second
|1 second

|===

== Design Decisions

=== Why FluentBit Forces Our Design

FluentBit's direct-insert model drives our architecture:

1. **No intermediate processor** - FluentBit inserts directly to storage
2. **Storage-side normalization** - Must use triggers (PostgreSQL) or transforms (Elasticsearch)
3. **Real-time processing** - No queuing or batch processing
4. **Idempotent operations** - Storage must handle duplicates and out-of-order events

This is a **constraint**, not a choice - FluentBit doesn't support intermediate processors.

=== Why Trigger/Transform-Based?

**Previous approach:**
- Separate Event Processor service
- Polled raw tables every N seconds
- Higher latency, more complex

**Current approach:**
- Storage-native normalization (triggers or transforms)
- No polling, no separate service
- Real-time processing, simpler architecture

=== Why JSONB/Object for Input/Output?

* **Flexible** - Workflow schemas vary by workflow
* **Queryable** - Can filter by JSON fields if needed
* **Compact** - Single column stores arbitrary JSON
* **GraphQL** - Exposed as String (clients parse JSON)

=== Why Separate Raw and Normalized Storage?

* **Raw storage** - Preserve original events for debugging
* **Normalized storage** - Optimized for querying
* **Idempotency** - Can replay raw events if normalization logic changes

== What Data Index Does NOT Do

[IMPORTANT]
====
Data Index is **read-only**. It does NOT:

* ❌ Execute workflows
* ❌ Modify workflow state
* ❌ Provide workflow management (start/stop/retry)
* ❌ Store workflow definitions
* ❌ Use polling (storage-native normalization)
====

== Choosing a Storage Backend

[cols="2,3,3"]
|===
|Requirement |PostgreSQL |Elasticsearch

|**ACID transactions**
|✅ Yes
|❌ Eventual consistency

|**Real-time (<1ms)**
|✅ Yes (triggers)
|⚠️ ~1s (transforms)

|**Full-text search**
|⚠️ Limited
|✅ Excellent

|**Throughput**
|< 50K workflows/day
|100K+ workflows/day

|**Complexity**
|⭐⭐ Medium
|⭐⭐⭐ Higher

|**Scaling**
|Vertical (single writer)
|Horizontal (distributed)

|===

== Next Steps

* xref:architecture/postgresql-mode.adoc[PostgreSQL Mode Details] - Trigger-based normalization
* xref:architecture/elasticsearch-mode.adoc[Elasticsearch Mode Details] - Transform-based normalization
* xref:deployment/overview.adoc[Deployment Guide] - Choose and deploy
