{
  "source": {
    "index": "workflow-events-*",
    "query": {
      "match_all": {}
    }
  },
  "dest": {
    "index": "workflow-instances"
  },
  "frequency": "1s",
  "sync": {
    "time": {
      "field": "@timestamp",
      "delay": "0s"
    }
  },
  "pivot": {
    "group_by": {
      "id": {
        "terms": {
          "field": "instanceId.keyword"
        }
      }
    },
    "aggregations": {
      "name": {
        "terms": {
          "field": "workflowName.keyword",
          "size": 1
        }
      },
      "version": {
        "terms": {
          "field": "workflowVersion.keyword",
          "size": 1
        }
      },
      "namespace": {
        "terms": {
          "field": "workflowNamespace.keyword",
          "size": 1
        }
      },
      "status": {
        "terms": {
          "field": "status.keyword",
          "size": 1,
          "order": {
            "priority": "desc"
          }
        },
        "aggs": {
          "priority": {
            "max": {
              "script": {
                "source": "String s = doc['status.keyword'].value; if (s == 'COMPLETED' || s == 'FAULTED' || s == 'CANCELLED') { return 3; } else if (s == 'RUNNING' || s == 'SUSPENDED') { return 2; } else { return 1; }"
              }
            }
          }
        }
      },
      "statusCount": {
        "value_count": {
          "field": "instanceStatus.keyword"
        }
      },
      "startDate": {
        "scripted_metric": {
          "init_script": "state.val = null",
          "map_script": "long ts = 0; if (params._source.startTime != null) { ts = ((Number)params._source.startTime).longValue() * 1000L; } else if (params._source.instanceStatus == 'STARTED' && params._source.eventTime != null) { ts = ZonedDateTime.parse(params._source.eventTime).toInstant().toEpochMilli(); } if (ts > 0 && (state.val == null || ts < state.val)) { state.val = ts; }",
          "combine_script": "return state.val",
          "reduce_script": "def min = null; for (s in states) { if (s != null && (min == null || s < min)) { min = s } } return min"
        }
      },
      "endDate": {
        "scripted_metric": {
          "init_script": "state.val = null",
          "map_script": "long ts = 0; if (params._source.endTime != null) { ts = ((Number)params._source.endTime).longValue() * 1000L; } else if ((params._source.instanceStatus == 'COMPLETED' || params._source.instanceStatus == 'FAULTED' || params._source.instanceStatus == 'CANCELLED') && params._source.eventTime != null) { ts = ZonedDateTime.parse(params._source.eventTime).toInstant().toEpochMilli(); } if (ts > 0 && (state.val == null || ts > state.val)) { state.val = ts; }",
          "combine_script": "return state.val",
          "reduce_script": "def max = null; for (s in states) { if (s != null && (max == null || s > max)) { max = s } } return max"
        }
      },
      "input": {
        "scripted_metric": {
          "init_script": "state.data = null; state.ts = 'ZZZZ'",
          "map_script": "if (params._source.input != null) { String ts = params._source.eventTime != null ? params._source.eventTime : String.valueOf(params._source.timestamp); if (ts != null && ts.compareTo(state.ts) < 0) { state.data = params._source.input; state.ts = ts } }",
          "combine_script": "return state",
          "reduce_script": "def earliest = ['data': null, 'ts': 'ZZZZ']; for (s in states) { if (s != null && s.get('ts') != null && s.ts.compareTo(earliest.ts) < 0) { earliest = s } } return earliest.data"
        }
      },
      "output": {
        "scripted_metric": {
          "init_script": "state.data = null; state.ts = ''",
          "map_script": "if (params._source.output != null) { String ts = params._source.eventTime != null ? params._source.eventTime : String.valueOf(params._source.timestamp); if (ts != null && ts.compareTo(state.ts) > 0) { state.data = params._source.output; state.ts = ts } }",
          "combine_script": "return state",
          "reduce_script": "def latest = ['data': null, 'ts': '']; for (s in states) { if (s != null && s.get('ts') != null && s.ts.compareTo(latest.ts) > 0) { latest = s } } return latest.data"
        }
      },
      "error": {
        "scripted_metric": {
          "init_script": "state.data = null; state.ts = ''",
          "map_script": "if (params._source.error != null) { String ts = params._source.eventTime != null ? params._source.eventTime : String.valueOf(params._source.timestamp); if (ts != null && ts.compareTo(state.ts) > 0) { state.data = params._source.error; state.ts = ts } }",
          "combine_script": "return state",
          "reduce_script": "def latest = ['data': null, 'ts': '']; for (s in states) { if (s != null && s.get('ts') != null && s.ts.compareTo(latest.ts) > 0) { latest = s } } return latest.data"
        }
      },
      "lastUpdate": {
        "max": {
          "field": "@timestamp"
        }
      }
    }
  }
}
