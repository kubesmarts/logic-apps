/**
 * This package has a blend between JPA and PostgreSQL implementation.
 * In the future, in case of support for other DBs, the packages `json`, and `storage` should be moved
 * to a `data-index-storage-jpa` module.
 */
package org.kubesmarts.logic.dataindex;